server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/databasename?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: xxxx
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE 